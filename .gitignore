# dependencies & builds
node_modules
.next
.turbo
dist
build

# env / secrets
.env
.env.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# misc
.DS_Store
*.zip

# TypeScript incremental build cache
*.tsbuildinfo

# Astro
dist/
.astro/

# env files (this project uses none, but never commit one if that changes)
.env
.env.*
!.env.example
